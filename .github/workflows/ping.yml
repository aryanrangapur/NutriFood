name: Ping Nutrifood Site

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl GET request
        run: |
          curl -I https://nutrifood.onrender.com/ || true
